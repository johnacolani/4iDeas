{
  "name": "four-ideas-rules-tests",
  "private": true,
  "description": "Firestore and Storage security rules tests for 4iCAD commerce.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project demo-4ideas \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.2.0",
    "firebase-tools": "^13.35.1"
  }
}